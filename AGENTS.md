This project aims to add to the GDM screen a type-as-you-find case insensitive
filter that allows users to quickly find their account name and log in. up and
down arrows should work normally, and enter should attempt to log in with the
highlighted user.

The input field should be placed above the user list.

There should be no change to screen lock, only the main GDM window.
